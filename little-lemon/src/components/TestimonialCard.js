function TestimonialCard(props) {
    return (
            <div style={{ display: "flex", gap: "26px", margin: "auto"}}>
                {props.card}
            </div>
    );
}

export default TestimonialCard;